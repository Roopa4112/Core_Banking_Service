spring.application.name=Account_Service
server.port=8082

# MariaDB connection
spring.datasource.url=jdbc:mariadb://localhost:3306/core_db
spring.datasource.username=Roopa
spring.datasource.password=Roopa@2114

# JPA (Hibernate) settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# Kafka Config
spring.kafka.bootstrap-servers=localhost:9092

# Producer (serialize POJOs to JSON)
#spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
#spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Consumer (deserialize JSON to POJOs)
#spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
#spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer

# Trusted packages (for deserialization of events)
#spring.kafka.consumer.properties.spring.json.trusted.packages=*

# Consumer group id
spring.kafka.consumer.group-id=account-service-group
